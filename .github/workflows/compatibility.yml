name: Compatibility

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions: {}

jobs:
  compatibility:
    permissions:
      contents: read
    uses: iwamot/workflows/.github/workflows/compatibility-python.yml@8a5a298fe570b32a00513bdbacddadb8fd1c0cfc # v3.0.0
    with:
      python-versions: '["3.11","3.12","3.13","3.14"]'
