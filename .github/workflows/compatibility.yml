name: Compatibility

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions: {}

jobs:
  compatibility:
    permissions:
      contents: read
    uses: iwamot/workflows/.github/workflows/compatibility-python.yml@cdc42cf2f91fd796de35bdc27aaaa1057695afcc # v6.1.0
    with:
      python-versions: '["3.11","3.12","3.13","3.14"]'
