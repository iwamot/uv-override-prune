name: Publish

on:
  push:
    tags: ['v*.*.*']

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    uses: iwamot/workflows/.github/workflows/publish-pypi.yml@e699662bcd64cc1247041af9160393a07b5bcac5 # v1.2.0
