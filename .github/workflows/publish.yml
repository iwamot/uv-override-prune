name: Publish

on:
  push:
    tags: ['v*.*.*']

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    uses: iwamot/workflows/.github/workflows/publish-pypi.yml@471bb812947616108999accbca3065012b71d221 # v1.1.0
